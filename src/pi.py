#!/usr/bin/env python

def func(x):
    f=float(4/1 + x²)
    return f

def defint(a,b,n,fx)
    valorpi = 0.0
    for i in range (0,n+1)
     valorpi +=  float(((b-a/n)/2*fx))
     return valorpi

 print def
